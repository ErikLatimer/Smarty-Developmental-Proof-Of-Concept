import {eur_usd_pane} from '../../render_panes/eur_usd_pane';

// The render pane modules should provide a React class element as a variable.

// The eur_usd_pane and all other imports will come with a method once its complete.
export var render_panes = {
    EUR_USD : eur_usd_pane
}

